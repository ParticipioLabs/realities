import React from 'react';
import { Alert } from 'reactstrap';

const App = () => (
  <div className="App">
    <Alert color="primary">
      We have Bootstrap 4!
    </Alert>
    <h1 className="App-title">Welcome to React</h1>
  </div>
);

export default App;
